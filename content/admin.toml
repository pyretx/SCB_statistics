# ─────────────────────────────────────────────────────────────────────────────
#  ADMIN PANEL CONTENT  —  edit the text here; no code changes needed.
#  Lines starting with #  are comments.  Text goes inside "quotes".
#  {placeholders} are filled in by the app — keep them where they appear.
# ─────────────────────────────────────────────────────────────────────────────

[header]
badge   = "ADMIN"                 # pill next to the wordmark
eyebrow = "Control room"
title   = "Admin panel"
back    = "Back to home"
gate    = "🔒 The admin panel is available to administrators only. Sign in with an admin account from the home page."
go_home = "Go to home"

[tabs]
overview = "Overview"
data     = "Data sources"
users    = "Users"

[overview]
kpi_users     = "Registered users"
kpi_countries = "Countries"
kpi_live      = "Live countries"
kpi_beta      = "Beta countries"
kpi_planned   = "Planned countries"
kpi_updates   = "Updates available"
kpi_beta_req  = "Beta requests"
upd_checked   = "Last checked {t}"
upd_never     = "No check run yet — run one from Data sources."
users_error   = "⚠ Could not load users: {err}"
open_heading  = "Open a country"
open_caption  = "Jump straight into any country page, including admin-only previews."
legacy_heading = "Decommissioned countries"
legacy_caption = "Legacy builds kept for admin reference — superseded by the framework pages above."

[data]
caption        = "Each source shows its latest data year and how it refreshes."
specific_heading = "Country-specific"
pill_ok        = "Up to date"
pill_update    = "Update available"
search_ph      = "🔍  Search sources — country, agency, classification…"
no_match       = "No data source matches “{q}”."
caches_title   = "Caches"
caches_type    = "Sweden · Norway · France live fetches"
caches_caption = "Live-API sources cache fetched figures to disk. Clear to force a re-fetch of the latest published data on next view."
clear_btn      = "🗑 Clear data caches"
cleared        = "Cleared — fresh figures on next view."

[data.us]
name        = "United States"
source      = "BLS OEWS"
type        = "bundled file"
s_year      = "Latest data year"
s_occ       = "Occupations"
s_scopes    = "Scopes"
s_size      = "File size"
desc        = "Bundled file built {built} · re-downloadable from BLS special-requests files (national + state + national-industry)."
alert       = "↻ May {latest} available — newer than the bundled May {year}."
btn_refresh = "↻ Refresh to May {year}"
btn_rebuild = "↻ Rebuild from BLS"
btn_check   = "🔍 Check for newer release"
confirm     = "This re-downloads ~65 MB from BLS and rebuilds the dataset (≈1–2 min). The running app updates immediately; to persist across redeploys, commit the regenerated `us_oews.json.gz`. ⚠ On the server this download fails — BLS blocks datacenter IPs — so run the refresh from the dev machine."
btn_confirm = "✅ Confirm refresh"
btn_cancel  = "Cancel"
refreshed   = "✅ Refreshed to May {year} · {occ} occupations · {scopes} scopes · {rows} rows · {size}"
failed      = "Last refresh failed: {err}"

[data.norway]
name    = "Norway"
source  = "SSB"
type    = "live API + STYRK labels"
s_year  = "Latest data year"
s_built = "STYRK labels built"
desc    = "Salaries fetched live from SSB table 11418 and disk-cached; the bundled bilingual STYRK labels give instant dropdowns. Rebuilding re-fetches the labels from SSB (EN + NO)."
btn     = "↻ Rebuild labels"
done    = "✅ Labels rebuilt {built} · {codes} codes ({leaves} occupations)."
# Data-year check (pinned year in app_settings.json, like Sweden)
btn_year   = "📅 Check data year"
y_fail     = "Couldn't reach SSB. Try again in a moment."
y_newer    = "SSB now publishes data up to **{found}** (app currently uses {cur})."
y_update   = "✅ Update to {found}"
y_keep     = "Keep {cur}"
y_uptodate = "Already up to date — SSB's newest year is {cur}."
y_older    = "SSB's newest reported year is {found}, older than the current setting ({cur}). Keeping {cur}."
y_done     = "✅ Data year updated to {found} — charts roll forward. Final step on the server: run `{cmd}` (~10 s, no rebuild) so the year-slider range picks it up."

[data.sweden]
name    = "Sweden"
source  = "SCB"
type    = "live API + stored codes & labels"
s_year  = "Latest data year"
s_codes = "SCB codes cached"
s_built = "SSYK labels built"
desc    = "Salaries fetched live from SCB; occupation codes + SSYK labels/translations are cached for fast, translated dropdowns. Re-fetching updates the occupation codes from the SCB API (EN + SV)."
btn     = "↻ Re-fetch codes"
done    = "✅ SCB codes re-fetched {ts}."
failed  = "Re-fetch failed: {err}"
# Data-year check (was the legacy page's "Check data year" dialog)
btn_year   = "📅 Check data year"
y_fail     = "Couldn't reach SCB. Try again in a moment."
y_newer    = "SCB now publishes data up to **{found}** (app currently uses {cur})."
y_update   = "✅ Update to {found}"
y_keep     = "Keep {cur}"
y_uptodate = "Already up to date — SCB's newest year is {cur}."
y_older    = "SCB's newest reported year is {found}, older than the current setting ({cur}). Keeping {cur}."
y_done     = "✅ Data year updated to {found} — charts, leaderboard and the work-permit check roll forward. Final step on the server: run `{cmd}` (~10 s, no rebuild) so the year-slider range picks it up."
btn_wp     = "⚙ Work permit rules"
extras_title = "Sweden (/sweden, framework) — features beyond the framework standard"
extras  = [
  "🛂 Work-permit check — Migrationsverket rules, salary floor, market position (country-specific extra tab)",
  "📖 SSYK descriptions & synonyms — shown in the code browser; synonyms searchable in the sidebar",
]

[data.france]
name      = "France"
source    = "INSEE"
type      = "live API + microdata + PCS labels"
s_year    = "Microdata year"
s_built   = "PCS labels built"
s_size    = "Microdata size"
desc      = "Mean-salary series fetched live from INSEE Melodi; per-occupation percentiles come from the bundled FD_SALAAN microdata, importable below when INSEE publishes a new vintage (PCS labels stay a bundled file)."
alert     = "↻ INSEE publishes annual data through {latest} — the bundled microdata is {year}."
btn_check = "🔍 Check INSEE for a newer year"
failed    = "INSEE check failed: {err}"
# Microdata import (country-specific): paste the INSEE parquet URL or upload
imp_caption    = "Import a new FD_SALAAN vintage: ① on insee.fr, find the newest “Description des emplois salariés — fichier détail” (search for FD_SALAAN), ② copy the .parquet file link — or download the file — and ③ paste the link or upload it below, then press Import. The file is ~100 MB; the import takes a few minutes."
imp_url_label  = "INSEE parquet URL"
imp_url_ph     = "https://www.insee.fr/fr/statistiques/fichier/…/FD_SALAAN_2024.parquet"
imp_file_label = "…or upload the downloaded .parquet"
imp_year_label = "Data year"
imp_btn        = "⬆ Import microdata"
imp_need_src   = "Paste an INSEE URL or upload the parquet file first."
imp_confirm    = "This replaces the bundled {cur} percentile estimates with {new}, computed from the file you provide. The current data is kept unless the new build validates successfully. To persist across redeploys, commit the regenerated pcs_microdata_percentiles.json."
btn_confirm    = "✅ Import now"
btn_cancel     = "Cancel"
imp_running    = "Importing microdata…"
imp_done       = "✅ Microdata imported: {year} · {occ} occupations ({censored} with a censored top band). Lives until the next redeploy — commit pcs_microdata_percentiles.json to make it permanent."
imp_failed     = "Import failed: {err}"
extras_title = "France (/france, framework) — features beyond the framework standard"
extras  = [
  "📈 Long-run trend 1996→ — constant-euro series per broad PCS group (real-only trend, no CPI overlay needed)",
  "👥 Population backdrop — the all-employee centile curve behind each salary-distribution chart",
  "🗺 Régional breakdown — mean by région at PCS-group level (occupation-level régional data is not published)",
  "🎂 Age bands — mean + headcount per age band from the annual Melodi dataset",
]

[users]
# ── Beta program requests (users ask from their profile dialog) ──
br_heading   = "Beta requests"
br_caption   = "Users who asked to join the beta program from their profile. Accept switches them to the Beta role; Decline keeps them Standard (they can ask again). You can always change any user's role later from the table below."
br_empty     = "No pending beta requests."
br_requested = "requested {date}"
br_accept    = "✅ Accept"
br_decline   = "Decline"
br_accepted  = "{email} is now a beta user."
br_declined  = "Declined — {email} stays standard."
br_failed    = "Could not update: {err}"
badge_requested = "beta requested"

create_heading = "Create user"
f_name         = "Name"
f_email        = "Email"
f_password     = "Password"
f_role         = "Role"
f_access       = "Country access"
ph_name        = "Full name"
ph_email       = "name@company.com"
btn_create     = "Create user"
missing        = "Name, email and password are required."
created        = "Created {email}"
create_failed  = "Could not create user: {err}"

existing_heading = "Existing users"
counts           = "{master} master · {admin} admin · {standard} standard · {total} total"
btn_reload       = "↻ Reload"
search_ph        = "🔍  Search users — name, email, role…"
no_match         = "No user matches “{q}”."
loading          = "Loading users…"
load_error       = "Could not list users: {err}"
retry_hint       = "Supabase can be slow to respond — try ↻ Reload."

col_user    = "User"
col_email   = "Email"
col_role    = "Role"
col_access  = "Access"
col_actions = "Actions"

no_name       = "— (no name)"
you           = " (you)"
all_countries = "All countries"
role_master   = "👑 Master"
role_admin    = "Admin"
role_beta     = "Beta"
role_standard = "Standard"

master_fixed  = "👑 Master — role fixed."
you_fixed     = "This is you — role fixed."
admin_access  = "Admins & master can open every country — no grants needed."
save_role     = "Save role"
save_access   = "Save access"
saved         = "Saved."
pw_yours      = "Your new password"
pw_new        = "New password"
btn_pw        = "Update password"
pw_updated    = "Password updated."
edit_help     = "Edit user"
del_help      = "Delete user"

# ── Global update check (Overview) — the results table + selected updates ─────
[updates]
btn_check     = "↻ Check all sources for updates"
check_help    = "Probes BLS, INSEE and SCB for newer data (runs only when pressed — never on page load)."
checking      = "Probing the data sources…"
heading       = "Data-source updates"
caption       = "Checked {t} · select the sources to update, then press Update selected."
col_select    = "Update"
col_country   = "Country"
col_source    = "Data source"
col_current   = "Current data"
col_latest    = "Latest available"
col_upd       = "Update available"
col_status    = "Status / notes"
v_yes         = "Yes"
v_no          = "No"
v_na          = "—"
btn_update    = "⬇ Update selected countries"
all_ok        = "✅ All data sources are up to date — nothing needs attention."
none_selected = "Tick at least one source in the Update column first."
confirm       = "About to update: **{names}**. Each source is replaced only after a successful download + validation — a failure leaves the current data untouched and does not stop the other selected sources."
btn_confirm   = "✅ Yes, update now"
btn_cancel    = "Cancel"
running       = "Updating…"
o_updated           = "✅ Updated"
o_up_to_date        = "✔ Already up to date"
o_manual            = "✋ Manual step required"
o_failed            = "❌ Update failed"
o_validation_failed = "⚠ Validation failed"
o_unavailable       = "🌐 Source unavailable"

[updates.notes]
# {cmd} = this environment's restart command (docker restart scb-dev/-test/-prod)
restart_step   = "Final step on the server: run `{cmd}` (~10 s, no rebuild) — the year-slider range picks the new year up then."
# {file} = the regenerated data file
commit_step    = "Runtime update — lives until the next redeploy; commit {file} to make it permanent."
us_backend     = "⚠ BLS blocks this server's IP — run this update from the dev machine (it works there), then commit us_oews.json.gz."
france_api     = "Live API — means & headcounts read INSEE Melodi directly."
france         = "Percentiles come from INSEE microdata (FD_SALAAN). Import the new vintage on the France source card: paste the INSEE parquet URL or upload the downloaded file."

# ── Work-permit rules editor (Sweden) — was the legacy page's admin editor ────
[wp]
back     = "← Back to data sources"
heading  = "Work permit rules · Sweden"
caption  = "The figures the Work-permit check uses (saved to wp_rules.json — overrides the built-in defaults and applies immediately across the app)."
f_asof            = "Rules valid from (YYYY-MM-DD)"
h_asof            = "Shown in the banner at the top of the Work permit tab."
f_median          = "National median salary (SEK)"
h_median          = "Migrationsverket's official median. Every % floor is computed from this number."
f_bench           = "Market benchmark year"
h_bench           = "SCB year used for the occupation market-position chart."
f_general         = "General floor (% of median)"
h_general         = "Default 90%. The standard salary floor."
f_transition      = "Transition floor (%)"
h_transition      = "Default 80%. Applies to extensions of permits granted before the reform, until the transition end date."
f_exempt          = "Exempt floor (%)"
h_exempt          = "Default 75%. Applies to the exempt SSYK list below."
f_blue            = "EU Blue Card threshold (SEK)"
h_blue            = "Fixed monthly threshold for the EU Blue Card route."
f_transition_end  = "Transition ends (YYYY-MM-DD)"
h_transition_end  = "From the day after this date the general floor applies even to extensions."
btn_save    = "💾 Save rules"
saved       = "Rules saved."
save_failed = "Could not save: {err}"
lists_heading = "SSYK code lists"
lists_caption = "Add/remove codes below, then press Save rules above to persist."
l_exempt_t         = "Exempt SSYK codes — 75% floor"
l_exempt_c         = "Occupations that use the 75% floor."
l_banned_full_t    = "Banned — full (hard block)"
l_banned_full_c    = "Cannot get a regular permit at all (e.g. 5343)."
l_banned_partial_t = "Banned — partial (warning)"
l_banned_partial_c = "Partly banned — shows a warning (e.g. 9210 berry pickers)."
none        = "— none —"
add_ph      = "e.g. 2512"
btn_add     = "➕ Add"
remove_help = "Remove"
